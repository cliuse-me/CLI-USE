# Propose Command

Use this command to engage the planner to propose a change and save it to a plan.
This will use the `cli-use-planner` persona.

Usage:
```bash
/propose <goal>
```

Example:
```bash
/propose Add a new authentication system
```