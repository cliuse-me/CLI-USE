# Implement Command

Use this command to engage the implementer to write code based on the latest saved plan.
This will use the `cli-use-implementer` persona.

Usage:

```bash
/implement
```

Example:

```bash
/implement Let's start with task 1
```
